# Master Secret for Content Encryption
# This is a 256-bit (32-byte) secret encoded in base64
# NEVER commit the actual .env file to version control!
# Generate using: python init_master_secret.py
MASTER_SECRET=your_base64_master_secret_here

# Database Configuration (optional, can use database.py defaults)
# DB_HOST=localhost
# DB_NAME=audiobooks
# DB_USER=root
# DB_PASSWORD=your_password_here

# Flask Configuration
# FLASK_ENV=production
# FLASK_DEBUG=False
